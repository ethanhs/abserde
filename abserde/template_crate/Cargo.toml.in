[package]
name = "{file}"
version = "0.1.0"
authors = ["{name} <{email}>"]
edition = "2018"

[dependencies]
pyo3 = "0.6"
serde = {{ version = "1.0", features = ["derive"] }}
serde_json = "1.0"
libc = "0.2.53"

[lib]
name = "{file}"
crate-type = ["cdylib"]
