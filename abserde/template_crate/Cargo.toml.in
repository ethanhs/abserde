[package]
name = "{file}"
version = "0.1.0"
authors = ["{name} <{email}>"]
edition = "2018"

[dependencies]
pyo3 = "*"
serde = {{ version = "1.0", features = ["derive"] }}
serde_json = "*"

[lib]
name = "{file}"
crate-type = ["cdylib"]
